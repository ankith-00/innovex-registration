<nav
    class="flex items-center justify-between px-4 md:px-[6em] py-6 bg-zinc-950 border-b border-zinc-800 sticky top-0 z-50">

    <div class="flex items-center gap-2">
        <h1 class="text-white font-bold tracking-tighter text-xl">INNOVEX</h1>
    </div>

    <div>
        <% if (currentPage==='/register' ) { %>
            <a href="/"
                class="bg-zinc-200 px-6 md:px-10 py-2 rounded-md font-medium text-sm text-black hover:bg-white transition-colors">
                Login
            </a>
            <% } else { %>
                <a href="/register"
                    class="bg-zinc-200 px-6 md:px-10 py-2 rounded-md font-medium text-sm text-black hover:bg-white transition-colors">
                    Register
                </a>
                <% } %>
    </div>
</nav>